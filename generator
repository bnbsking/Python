#data structure
#generator vs list

def g(n):
    i = 0
    while i < n:
        yield i     # append i in a Generator g(n) (dynamic data structure)
                    # next time calling g(n) execute from here
        i+=1

for i in g(3):      # generate an element while vist the element, O(n)
                    # compare to list: generate all elements than visit all elements, O(2*n)
    print(i)
    
p = g(3)
print(next(p))      # 0
print(next(p))      # 1
print(next(p))      # 2
print(next(p))      # error
